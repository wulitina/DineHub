spring:
  application:
    name: order
  profiles:
    active: dev
  cloud:
    config:
      uri:  http://localhost:8050
      fail-fast: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/